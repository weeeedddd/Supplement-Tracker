name: Validate and deploy Pages

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
    steps:
      - name: Check out source
        uses: actions/checkout@v6
      - name: Use Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
          cache-dependency-path: frontend/package-lock.json
      - name: Resolve patched dependency metadata
        working-directory: frontend
        run: npm view nanoid@3.3.18 dist --json
      - name: Install locked dependencies
        working-directory: frontend
        run: npm ci --ignore-scripts
      - name: Audit dependencies
        working-directory: frontend
        run: npm audit --audit-level=high
      - name: Run regression tests
        working-directory: frontend
        run: npm test
      - name: Build production app
        working-directory: frontend
        run: npm run build
      - name: Configure Pages
        if: github.event_name != 'pull_request'
        uses: actions/configure-pages@v6
      - name: Upload Pages artifact
        if: github.event_name != 'pull_request'
        uses: actions/upload-pages-artifact@v5
        with:
          path: frontend/dist

  backend:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Check out source
        uses: actions/checkout@v6
      - name: Use Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'
          cache: pip
          cache-dependency-path: backend/requirements-integrations-dev.txt
      - name: Install integration dependencies
        working-directory: backend
        run: python -m pip install --requirement requirements-integrations-dev.txt
      - name: Lint integration boundary
        working-directory: backend
        run: python -m ruff check app/api_v1.py app/integration_app.py app/integration_models.py app/integrations.py app/security.py tests/test_integrations.py
      - name: Test integration boundary
        working-directory: backend
        run: python -m pytest -q

  deploy:
    if: github.event_name != 'pull_request'
    needs: [build, backend]
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
